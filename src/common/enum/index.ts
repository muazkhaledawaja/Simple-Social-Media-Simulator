/* eslint-disable prettier/prettier */
export enum ROLES {
    ADMIN = 'admin',
    USER = 'user',
  }
  
  export enum RequestStatus {
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    DECLINED = 'declined',
  }